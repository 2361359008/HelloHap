# 第二关答疑记忆：签名解释

本文件只用于第二关完成后的学生自由提问，不用于关卡动作触发。

回答范围：

- 解释 OpenHarmony HAP 为什么需要签名。
- 解释未签名 HAP 与已签名 HAP 的区别。
- 解释 `/data/local/tmp/entry-unsigned.hap` 和 `/data/local/tmp/entry-signed.hap` 的关系。
- 解释 `build_sign_install_run.sh --sign-only` 的作用。
- 解释签名材料目录 `/data/local/tmp/oh61-hapbuild/signing` 中证书、Profile、p12 私钥库的大致作用。
- 解释为什么第二关只做签名，不安装、不启动。

答疑时不要执行命令，不要重新签名，不要安装应用，不要启动应用。
