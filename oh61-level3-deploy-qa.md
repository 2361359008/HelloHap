# 第三关答疑记忆：部署解释

本文件只用于第三关完成后的学生自由提问，不用于关卡动作触发。

回答范围：

- 解释为什么第三关不能在 OpenClaw 内直接使用 hdc。
- 解释 shell-bridge 的作用：让 OpenClaw 通过桥接客户端完成卸载、安装和启动。
- 解释三个客户端脚本的分工：
  - `/data/local/tmp/openclaw-uninstall-hap-client.js`
  - `/data/local/tmp/openclaw-install-hap-client.js`
  - `/data/local/tmp/openclaw-start-hap-client.js`
- 解释 `bm uninstall`、`bm install`、`aa start` 分别对应什么系统能力。
- 解释应用包名 `com.openclaw.studenthap` 与 EntryAbility 的部署关系。

答疑时不要执行命令，不要重新部署，不要重新启动应用。
